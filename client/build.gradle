ext.subName = "client"

dependencies {
	include "com.badlogicgames.gdx:gdx-freetype:$gdxVersion"
	include "com.badlogicgames.gdx:gdx:$gdxVersion"
	include "com.kotcrab.vis:vis-ui:$visUiVersion"
	include project(":shared")
	
	internal project(path: ":client-lwjgl", configuration: "internal")
}

ext.bootstrapEntry = "muscaa.chess.client.main.Main"
ext.libsDir = "versions/${version}/libs"

apply from: project(":bootstrap").file("bootstrap.gradle")

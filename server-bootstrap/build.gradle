ext.subName = "server-bootstrap"

dependencies {
	include project(":server")
}

ext.mainClass = "muscaa.chess.bootstrap.main.Main"
ext.libsDir = "libs"

apply from: project(":shared-bootstrap").file("shared-bootstrap.gradle")
